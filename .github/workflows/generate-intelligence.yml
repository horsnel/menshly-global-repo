# ============================================================
# Menshly Global — Auto Generate Intelligence Articles
# Posts deep implementation guides — the full stack, every resource
# Target: content/intelligence/
# ============================================================

name: Generate Intelligence Article

on:
  schedule:
    # Runs at 12:00 PM UTC daily (1 PM WAT)
    - cron: '0 12 * * *'
  workflow_dispatch:
    inputs:
      topic:
        description: 'Specific topic (leave empty for auto-pick)'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  generate-intelligence:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.actor != 'github-actions[bot]'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Generate Intelligence article
        env:
          AI_API_KEY: ${{ secrets.AI_API_KEY }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
          AI_API_BASE: ${{ secrets.AI_API_BASE }}
          AI_MODEL: ${{ secrets.AI_MODEL }}
          MANUAL_TOPIC: ${{ inputs.topic }}
        run: python scripts/generate-intelligence.py

      - name: Check if article was created
        id: check
        run: |
          if [ -s output/article.md ]; then
            echo "created=true" >> $GITHUB_OUTPUT
            echo "Intelligence article generated successfully"
          else
            echo "created=false" >> $GITHUB_OUTPUT
            echo "No article was generated"
          fi

      - name: Commit and push article
        if: steps.check.outputs.created == 'true'
        run: |
          SECTION=$(cat output/target_section.txt)
          SLUG=$(cat output/target_slug.txt)
          git config user.name "Menshlyglobal Bot"
          git config user.email "bot@menshly-global.pages.dev"
          cp output/article.md "content/${SECTION}/${SLUG}.md"
          git add "content/${SECTION}/${SLUG}.md"
          git commit -m "Auto: Intelligence article ${SLUG} [skip ci]"
          git push
